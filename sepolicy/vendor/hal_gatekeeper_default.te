# mindone (06.09): the AIDL gatekeeper -> HIDL gatekeeper@1.0 bridge lives in the same domain as the HIDL service
# (AOSP: hal_server_domain(hal_gatekeeper_default, hal_gatekeeper)); the bridge is additionally a HIDL CLIENT.
hal_client_domain(hal_gatekeeper_default, hal_gatekeeper)
